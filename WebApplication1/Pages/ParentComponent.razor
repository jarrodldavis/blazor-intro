@page "/ParentComponent"
@page "/ParentComponent/{year:int}"

<h1>Parent-child example</h1>

<ChildComponent Title="Panel Title from Parent"
                OnClick="@ShowMessage"
                bind-Year="@Year">

    Content of the child component is supplied by the parent component.
</ChildComponent>

<p><b>@messageText</b></p>
<p><b>parent year: @Year</b></p>

@functions {
    [Parameter]
    private int Year { get; set; } = 1978;

    private string messageText;

    private void ShowMessage(UIMouseEventArgs e) {
        messageText = "Blaze a new trail with Blazor!";
        Year = 1986;
        Console.WriteLine(Json.Serialize(e));
    }
}
